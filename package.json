{
  "name": "foco.frontend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:full": "concurrently --kill-others-on-fail --raw --names backend,frontend --prefix \"{time} [{name}]\" --prefix-colors blue,magenta \"npm run dev --prefix ../foco.backend\" \"npm run dev\"",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "vite": "^8.2.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.3",
    "sortablejs": "^1.15.7"
  }
}
